/**
 * in client side, 
 * If user is authenticated → render children
 * If user is not authenticated → redirect, show fallback, or block UI
 * It is auth-specific UI logic
 * It enforces auth rules at render time
 */


